import { ExctractvaluePipe } from './exctractvalue.pipe';

describe('ExctractvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new ExctractvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
